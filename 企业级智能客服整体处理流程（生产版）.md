# 企业级智能客服整体处理流程（生产版）

## 总体流程图

``` text
用户
 ↓
文本预处理
 ↓
Intent Router
 ├── Rule Engine
 ├── Embedding Classifier
 └── LLM Classifier
        ↓
 IntentResult
        ↓
 ├── FAQ → LangCache
 ├── 业务查询 → Agent Tool → Java API
 └── 人工 → 转人工

LangCache
 ├── Hit → 返回
 └── Miss
        ↓
RAG(BM25 + Vector + Rerank)
        ↓
Prompt组装
        ↓
LLM(DeepSeek/Qwen)
        ↓
返回答案
        ↓
写回 LangCache
```

## IntentResult

``` json
{
  "intent":"reward_not_arrived",
  "confidence":0.96,
  "allow_cache":false,
  "need_user_data":true,
  "need_rag":true,
  "need_human":false,
  "slots":{
      "activity_type":"ETC"
  }
}
```

## 模块职责

### 文本预处理

-   去空格
-   拼写纠错
-   同义词归一
-   敏感词过滤

### Rule Engine

-   关键词
-   正则
-   排除词
-   词典
-   优先级
-   JSON配置
-   热更新

规则保存在 MySQL/Redis 中，Python 只负责解析执行。

### LangCache

仅缓存： - FAQ - 产品介绍 - 活动规则 - 固定政策

禁止缓存： - 用户余额 - 权益值 - 奖励到账状态 - 实时订单 - 实时库存

### Agent Tool

统一调用 Java API： - 查询订单 - 查询权益 - 查询奖励 - 查询活动

### RAG

-   BM25
-   Vector Search
-   Rerank

### 大模型

负责最终推理、Tool Calling、自然语言生成。

## 推荐技术栈

``` text
Vue3
 ↓
Python FastAPI
 ↓
Intent Router
 ↓
Rule Engine
 ↓
LangCache(Redis Vector)
 ↓
Agent
 ↓
RAG
 ↓
DeepSeek/Qwen
 ↓
Java Business API
 ↓
MySQL / Redis / Kafka
```

## 设计原则

1.  规则解决确定性问题。
2.  Embedding 做语义分类。
3.  LLM 负责复杂推理。
4.  LangCache 只缓存稳定、非个人化答案。
5.  Python 不直接访问业务数据库，通过 Java API 获取业务数据。
6.  所有规则配置化、热更新、灰度发布。
7.  全链路日志监控持续优化。
